import React from 'react';
import { StyledBottomBar, Button } from './styles';

const BottomBar = () => (
  <StyledBottomBar>
    <span></span>
    <Button>Done</Button>
  </StyledBottomBar>
);

export default BottomBar;
